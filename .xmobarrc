Config { font = "xft:Bitstream Vera Sans Mono:pixelsize=13:scalable=true:antialias=true",
         bgColor = "black",
         fgColor = "grey",
         position = TopW L 90,
         commands = [ Run Date "%a %b %e %H:%M" "date" 60,
                      Run StdinReader ],
         sepChar = "%",         
         alignSep = "}{",
         template = "%StdinReader% }{ <fc=grey>%date%</fc>"
       }
